(() => {
  const body = document.getElementsByTagName("body")[0];

  const container = document.createElement("div");
  Object.assign(container.style, {
    opacity: 1,
    width: "100%",
    height: "100%",
    left: "0px",
    top: "0px",
    position: "fixed",
    zIndex: "999999",
    backgroundColor: "black",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
  });

  const message = document.createElement("div");
  message.innerHTML = `
  <span style="color: red;">H</span> 
  <span style="color: green;">e</span> 
  <span style="color: yellow;">l</span> 
  <span style="color: brown;">l</span> 
  <span style="color: blue;">o</span> 
  <span style="color: pink; margin-left: 20px;">W</span> 
  <span style="color: aliceblue;">o</span> 
  <span style="color: darkseagreen;">r</span> 
  <span style="color: darkgoldenrod;">l</span> 
  <span style="color: purple;">d</span> 
 `;
  Object.assign(message.style, {
    fontSize: "60px",
    color: "white",
  });

  const description = document.createElement("div");
  description.innerText = 'This is generated by the example script of the extension "ScriptInjector"';
  Object.assign(container.style, {
    fontSize: "18px",
    color: "white",
  });

  container.appendChild(message);
  container.appendChild(description);
  body.appendChild(container);

  let counter = 0;
  setTimeout(() => {
    let interval = setInterval(() => {
      counter++;
      if (counter > 200) {
        if (container.parentElement) {
          // container.parentElement.removeChild(container);
        }
        clearInterval(interval);
        interval = undefined;
      }
      container.style.opacity = Math.max(0, 1 - counter / 100);
    }, 20);
  }, 3000);
})();